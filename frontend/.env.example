# Public browser URL for the backend API (NestJS default port).
NEXT_PUBLIC_API_URL=http://localhost:3000

# Optional server-side URL for Next.js server components/actions.
# Docker Compose uses the backend service name (set in docker-compose.yml):
# INTERNAL_API_URL=http://backend:3000
# Vercel/Railway split deploys can usually omit this and use NEXT_PUBLIC_API_URL.

# Public app URL — used for Open Graph / metadata
NEXT_PUBLIC_APP_URL=http://localhost:3001

# GitHub repo link on the login page
NEXT_PUBLIC_GITHUB_REPO_URL=https://github.com/nkieu-config/branchbrew-cafe-erp

# Playwright E2E (optional — defaults work for local dev)
# PLAYWRIGHT_BASE_URL=http://localhost:3001
# PLAYWRIGHT_SKIP_WEBSERVER=1
# E2E_EMAIL=manager@branchbrew.dev
# E2E_PASSWORD=password123
