# Committed CI / smoke-test env (non-production secrets only).
JWT_SECRET=test-jwt-secret-for-ci-only-32chars
NEXT_PUBLIC_API_URL=http://localhost:3000
POSTGRES_USER=erp_user
POSTGRES_PASSWORD=erp_password
POSTGRES_DB=erp_db
